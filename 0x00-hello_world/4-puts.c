#include <stdio.h>
/**
 * a program that prints a line with puts function
 * main: entry level point
 * Return: 0(success)
 */
int main(void)
{
	puts(""Programming is like building a multilingual puzzle"/n;
		return(0);
		}

